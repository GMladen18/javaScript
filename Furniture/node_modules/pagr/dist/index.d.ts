export interface Page {
    current?: boolean;
    page: number;
    type: 'page';
}
export interface Separator {
    type: 'separator';
}
declare const pagr: (current: number, bound: number, max: number, separator?: string) => Array<Page | Separator>;
export default pagr;
